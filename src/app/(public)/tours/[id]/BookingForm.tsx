// Deprecated: moved to src/features/booking/components/BookingForm.tsx
// (split into step components for modularity). Kept as a re-export — see
// note in src/lib/mongodb.ts.
export { default } from '@/features/booking/components/BookingForm'
