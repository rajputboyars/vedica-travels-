// Deprecated: moved to src/features/tours/components/TourImage.tsx (it's
// tour-domain-specific, not a generic primitive). Kept as a re-export —
// see note in src/lib/mongodb.ts.
export { default } from '@/features/tours/components/TourImage'
